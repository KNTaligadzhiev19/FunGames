#pragma once

// better practice is for every file a header file to be created with the same name and not one that contains all the functions from different files
// header files are to contain only functions used across multiple files
// helps with minimizing dependencies and compile time as well as abstraction (one of the key principles of OOP)

int startTicTacToe();